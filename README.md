# 双生秘法：Arcane Bond

本地双人同屏像素动作游戏。两名玩家共用一套键盘，一个人写下控制与标记，另一个人触发连携并完成终结。

## 启动

在 macOS 中双击 `play.command`。启动器会从 `4173–4183` 中选择可用端口，只在 `127.0.0.1` 上运行，并自动打开浏览器。

也可以在终端运行：

```bash
python3 tools/serve.py
```

游戏运行时不访问互联网。由于浏览器会限制 `file://` 下的图片和声音，请不要直接双击 `index.html`。

## 操作

| 功能 | 玩家 A · 辅助位 | 玩家 B · 输出位 |
| --- | --- | --- |
| 移动 | W / A / S / D | 方向键 |
| 普通攻击 | F | J |
| 技能 1 | G | K |
| 技能 2 | H | L |
| 终极技能 | T | O |
| 唤醒 / 祭坛确认 | E | P |

首次游玩会进入四步双生试炼。星辉与影焰默认开放；首次通关解锁森语巫师，击败荆棘母树解锁雷鸣巫师。

## 当前内容

- 4 位巫师与 4 种搭档共鸣语法。
- 8 类小怪、12 个随机遭遇模板、3 个双阶段 Boss。
- 每局 6 个战斗房、2 次共同祝福、1 个随机事件和 1 场 Boss 战。
- 24 个规则型祝福，不包含永久伤害或生命成长。
- 法术笔法、Boss 词缀、三档难度与法袍外观组成的横向解锁。
- 本地版本化存档、秘法图鉴、设置和损坏存档回退。
- 15 套角色精灵、8 套特效、原创图块、2 首音乐和 12 个音效。
- 停帧、残影、闪白、震屏、预警与 Boss 阶段转场。

## 技术结构

- `src/scenes/`：Phaser 启动、菜单、故事、选角、教程、冒险、奖励、图鉴和结算场景。
- `src/content/`：巫师、祝福、敌人、Boss 与遭遇注册表。
- `src/systems/`：种子随机数、战斗规则、存档、音频、UI 与单局状态。
- `assets/`：完全本地的 PNG 和 WAV 资产。
- `tools/generate_assets.py`：确定性生成原创 8 位资产。

运行测试：

```bash
npm test
```

## Phaser

运行时使用 Phaser `3.90.0`，已固定在 `vendor/phaser.min.js`，不依赖 CDN。

SHA-256：`e92ddef111ba42e92d316979c732311757093688ea1810591cb7aa2858eba7a7`

Phaser 许可证保存在 `vendor/PHASER-LICENSE.md`。
